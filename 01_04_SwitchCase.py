from math import pi

class Solution:
    def switchCase(self, choice, arr):
        # Code here
        if choice == 1:
           return(pi * arr[0] * arr[0]);
        else:
           return((arr[0] * arr[1]));
